version: 2
jobs:
   build:
     docker:
       - image: circleci/python:2.7
     steps:
       - checkout
       - run: echo "hello world"

workflows:
  version: 2
  prtest:
    jobs:
      - build:
          filters:
            branches:
              ignore:
                - prtest
